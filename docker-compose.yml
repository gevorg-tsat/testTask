version: '2'
services:
    web:
        build: .
        ports:
            - "8001:8001"
        volumes:
            - .:/code
        depends_on:
            - redis
    redis:
        image: redis